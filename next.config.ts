import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: 'export', // Commented out for development
  // basePath: '/maryam-bakkar', // Commented out for development
  images: {
    unoptimized: true
  },
  // trailingSlash: true,
};

export default nextConfig;
